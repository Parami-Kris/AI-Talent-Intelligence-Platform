name: Deploy backend to Hugging Face Spaces

on:
  push:
    branches: [main]
    paths:
      - "backend/**"
      - "pipeline/**"
      - "requirements.txt"
      - "Dockerfile"
      - ".github/workflows/deploy-backend-hf-space.yml"
  workflow_dispatch: {}

jobs:
  sync-to-hub:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          lfs: true

      # HF Spaces rejects binary files that aren't tracked via git-lfs/Xet.
      # Rewriting this only in the CI checkout keeps origin's history untouched.
      - name: Move binary files to LFS for the Space push
        run: |
          git lfs install
          git lfs migrate import --include="Parami_Resume.pdf" --everything

      - name: Push to Hugging Face Space
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
          HF_SPACE: ${{ vars.HF_SPACE }}
        run: |
          git remote add space "https://user:${HF_TOKEN}@huggingface.co/spaces/${HF_SPACE}"
          git push --force space HEAD:main
